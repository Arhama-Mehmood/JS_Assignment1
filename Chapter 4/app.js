// Q1)
var name, father_name, gender;

// Q2)
// Legal Variable Names:
var name, father_name, subject_1, $subject_2, title;

// Illegal Variable Names:
//  var 1name , @class , 84Gamepad , *hint , %title ;  [I commented it because if I didn't do it then because of error my other code will not run.]

// Q3)
document.write("<h2>Rules For naming JS variables </h2><br><br>");
document.write("Variable names can only contain numbers, $ and _. For example :$my_1stVariable. <br>");
document.write("Variable must begin with a letter, $ or an underscore _ . For example name, &name, _name.<br>");
document.write("Variable names are case sensitive.<br>");
document.write("Variable names should not be JS keywords.");
